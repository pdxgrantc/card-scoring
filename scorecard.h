#ifndef SCOREBOARD
#define SCOREBOARD

#include <string>

using namespace std;

class Scorecard
{
protected:
    int numPlayers;

public:
    Scorecard();
    /*
    int takeNumPlayers();
    bool isNumber(const string &str);
    */
};

#endif